defmodule VolleyWeb.PageControllerTest do
  use VolleyWeb.ConnCase

  # Out of date and not ready to test
  # test "GET /", %{conn: conn} do
  #   conn = get(conn, ~p"/")
  #   assert html_response(conn, 200) =~ "Peace of mind from prototype to production"
  # end
end
